services:
  meyro-engine:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: meyro-engine
    ports:
      - "8000:8000"
    environment:
      - MEYRO_ENV=production
      - LOG_LEVEL=info
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    restart: unless-stopped
