# ---------------------------------------------------------------------------
# MEYRO — development dependencies
#
# Intentionally does NOT include `-r requirements.txt`, so that Phase 0
# environments stay lightweight. Runtime deps are installed per phase.
# ---------------------------------------------------------------------------

# --- Testing ---
pytest>=8.3
pytest-cov>=5.0

# --- Lint & format ---
ruff>=0.6

# --- Static typing ---
mypy>=1.11
types-PyYAML>=6.0.12
pandas-stubs>=2.2

# --- Git hooks ---
pre-commit>=3.8
